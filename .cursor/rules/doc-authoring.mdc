---
description: 撰写 docs 下 Markdown 文档时的详细写作与排版约定
globs: docs/**/*.md
---

# 文档撰写细则（docs/**/*.md）

编辑 `docs/` 下的 Markdown 文档时，除全局规范外，还需遵循：

## 内容质量（面向零基础读者）

- **通俗优先、专业对齐**：每个专业概念/专有名词，先用大白话或类比讲清"是什么、为什么、解决什么问题"，再给公式/定义，最后把专业表述回扣到前面的大白话（"这个公式就是前面那句……"）。严禁一上来就甩定义和公式。
- **术语先解释再使用**：专有名词首次出现立刻用括号或小注解释。
- **多用比喻/例子/对比表格**：能用生活场景、"朴素做法 vs 更好做法"表格、文字流程图说明的，优先于大段专业论述。
- **案例可复现**：给出数据、参数与期望结果；涉及代码时放入 `code/` 镜像目录并在文中相对链接引用。
- **正确性优先**：宁可少写，不可写错；关键结论应对应教科书/论文/官方文档共识。存疑处显式标注。
- **黄金样板**：对照 `docs/03-深度学习基础/001-神经网络结构.md` 的"为什么必须有非线性激活函数"一节的通俗写法。

## 排版

- 使用二级标题分节（`## 一、`…），层级清晰，避免超过三级标题嵌套。
- 数学公式使用 LaTeX：行内 `$...$`，独立公式 `$$...$$`（站点已启用 mathjax3）。
- 知识图谱/流程图使用 Mermaid 代码块，节点用中文知识点名，箭头表达依赖/演进/组成关系，配合 `classDef` 分层着色。
- 表格用于对比多个概念/参数；避免大段无结构文字。

## 链接与引用

- 同分类内引用其它文档用相对路径，如 `[001 · 神经网络结构](./001-神经网络结构.md)`。
- 跨分类引用用相对路径回溯，如 `../03-深度学习基础/000-分类总览与知识图谱.md`。
- 站点开启了 `ignoreDeadLinks: false`，失效链接会导致构建失败，务必校对。

## 每个分类的 000 文档

- 必须包含：本分类学什么、通俗理解、知识图谱（Mermaid）、学习建议、小结。
- 知识图谱需真实反映本分类各知识点之间的关系，而非罗列节点。
