---
description: AI 学习知识库的全局文档规范（分类、命名、质量、图谱）
alwaysApply: true
---

# AI 学习知识库 · 全局规范

本仓库是用于系统学习 AI 开发的知识库，以文档为主、含少量学习/测试代码。处理任何文档任务时，必须遵守以下规范（权威版本见 `docs/00-元规范/`）。

> 目标读者是 **AI 开发零基础的初学者**。"小白能否读懂"是最高优先级；术语堆砌、只给专业定义的文档视为不合格。

## 六条硬性要求

1. **系统性分类**：文档必须放入 `docs/` 下的「两位数序号-分类名」目录（如 `03-深度学习基础`），不得游离。
2. **章节序号命名**：分类内文档以三位序号开头，如 `001-神经网络结构.md`；每个分类必须含 `000-` 总览/知识图谱文档。
3. **全面且正确**：覆盖知识点核心要素；公式、结论、数据必须可追溯、可验证、正确无误，不确定处需标注。
4. **教授级 + 案例**：以大学教授级严谨度讲解，配可复现的真实案例（经典案例优先，如 XOR、MNIST）。
5. **通俗优先、专业对齐（头等要求）**：面向零基础读者，**每个专业概念/专有名词都要先用大白话或类比讲清"是什么、为什么、解决什么问题"，再给严谨定义，最后把专业表述与通俗讲法显式对齐**（"这个公式就是前面那句大白话"）。术语"先解释再使用"，多用比喻/例子/对比表格；严禁上来就甩公式术语。类比不得引入错误直觉。

## 通俗优先三步法（写每个概念都照做）

1. **先大白话**：一句"人话"说清本质；专有名词首次出现立刻用括号解释。
2. **打比方 + 讲"解决什么问题"**：生活化类比 + 拆成「问题1/问题2/…」，每个配具体例子或"朴素做法 vs 更好做法"对比表格。
3. **专业对齐**：再给定义/公式，并立刻回扣前面的大白话。

黄金样板：`docs/03-深度学习基础/001-神经网络结构.md` 的"为什么必须有非线性激活函数"一节，改写/新写文档时对照它。

6. **分类总结含知识图谱**：每个分类的 `000-` 文档需串联本分类知识点，并用 Mermaid（```mermaid）绘制知识图谱。

## 目录与命名

- 文档：`docs/NN-分类名/NNN-知识点.md`
- 配套代码：`code/NN-分类名/NNN-知识点/`（镜像文档目录）
- 新增文档后运行 `npm run docs:validate` 校验命名与结构。

## 单篇文档结构（推荐）

`一、直觉与通俗解读` → `二、严谨定义与原理` → `三、案例解析` → `四、常见误区与边界` → `五、小结与延伸阅读`。

## 校验与预览

- 提交前：`npm run docs:validate`（lint）+ `npm run dev`（预览，确认公式/图谱/链接正常）。
- 遇到概念不确定，优先用 `deepwiki` / `context7` MCP 查权威来源核实，确保正确性。
