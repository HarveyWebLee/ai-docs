# 003 · 模型评估与指标

> 本文回答：为什么"准确率高"可能是陷阱？精确率、召回率、F1、AUC 各衡量什么？如何用交叉验证可靠地评估模型？

## 一、直觉与通俗解读

评估模型就像**考核一个医生**：

- **准确率**：判对的比例。但如果 99% 的人是健康的，一个"永远说健康"的庸医准确率也有 99%——**准确率在类别不平衡时会骗人**。
- **精确率（Precision）**：医生说"你有病"的人里，真有病的比例（**别乱报警**）。
- **召回率（Recall）**：真正有病的人里，被医生查出来的比例（**别漏诊**）。
- **F1**：精确率和召回率的平衡分。

精确率和召回率往往**此消彼长**：想少漏诊就容易多误报，反之亦然。

## 二、严谨定义与原理

### 2.1 混淆矩阵

二分类的四种结果：TP（真正例）、FP（假正例）、TN（真负例）、FN（假负例）。

$$
\text{准确率} = \frac{TP+TN}{TP+TN+FP+FN}
$$

$$
\text{精确率} = \frac{TP}{TP+FP},\qquad
\text{召回率} = \frac{TP}{TP+FN}
$$

$$
F_1 = 2\cdot\frac{\text{精确率}\cdot\text{召回率}}{\text{精确率}+\text{召回率}}
$$

F1 是精确率与召回率的**调和平均**，只有二者都高时 F1 才高。

### 2.2 ROC 与 AUC

改变分类阈值会得到不同的 (FPR, TPR) 点，连成 **ROC 曲线**；曲线下面积 **AUC** 衡量模型的整体排序能力：AUC=1 完美，AUC=0.5 等同随机猜测。AUC 对阈值不敏感，适合比较模型。

### 2.3 数据划分与交叉验证

- **训练/验证/测试划分**：训练集学参数，验证集调超参，**测试集只在最后用一次**评估泛化。
- **K 折交叉验证**：把数据分成 K 份，轮流用 1 份验证、其余训练，取平均。小数据集上更稳健、更充分利用数据。

### 2.4 回归指标

- 均方误差 MSE、平均绝对误差 MAE、决定系数 $R^2$（越接近 1 越好）。

## 三、案例解析：不平衡数据下的"准确率陷阱"

某疾病患病率仅 1%。测试集 1000 人（10 病 990 健康）。一个"全判健康"的模型：

- 准确率 $=\dfrac{990}{1000}=99\%$，看似惊艳；
- 但 TP=0，召回率 $=\dfrac{0}{10}=0\%$——**一个病人都没查出来**，毫无价值。

改用 F1 或召回率评估，立刻暴露该模型无效。**这说明指标必须匹配业务目标**：疾病筛查更看重召回率（宁可误报、不可漏诊），而垃圾邮件拦截更看重精确率（宁可漏拦、不可误杀正常邮件）。

## 四、常见误区与边界

- **误区："唯准确率论"**：类别不平衡时准确率极具误导性。
- **误区："在测试集上反复调参"**：这会导致信息泄漏、测试集失去评估意义；调参用验证集。
- **误区："AUC 高就一定好"**：AUC 衡量排序能力，但业务需要的是特定阈值下的表现，需结合 PR 曲线。

## 五、小结与延伸阅读

- 用混淆矩阵派生精确率/召回率/F1；不平衡数据别只看准确率。
- ROC-AUC 衡量排序能力；用交叉验证获得稳健评估；测试集只用一次。
- 下一步：[004 · 过拟合与正则化](./004-过拟合与正则化.md)，解决"训练好、测试差"的问题。
