# 002 · 分词与子词切分

> 本文回答：中文为什么需要分词？子词切分（BPE）如何解决未登录词问题？为什么现代大模型都用子词 token？

## 一、直觉与通俗解读

模型不能直接吃"一整句话"，得先切成一个个**token（词元）**。怎么切是门学问：

- **英文**天然有空格，但仍有 `playing = play + ing`、罕见词、拼写变体的问题。
- **中文**没有空格，"研究生命科学"可以切成"研究 / 生命 / 科学"，也可能被错切成"研究生 / 命 / 科学"——**切错语义全变**。
- **子词切分**是折中方案：把词拆成更小的"零件"，常见词整体保留，生僻词拆成子词。这样既控制了词表大小，又能拼出任何没见过的词（**未登录词**）。

## 二、严谨定义与原理

### 2.1 中文分词的挑战

- **歧义切分**："乒乓球拍卖完了"可切成"乒乓球拍/卖完了"或"乒乓球/拍卖/完了"。
- **未登录词（OOV）**：新词、人名、专有名词层出不穷。

经典方法：基于词典的最大匹配、基于统计的隐马尔可夫模型（HMM）/条件随机场（CRF）、以及基于神经网络的序列标注。

### 2.2 子词切分：BPE

**字节对编码（Byte Pair Encoding, BPE）** 的核心思想：

1. 初始把文本拆成最小单元（字符/字节）。
2. 统计相邻单元对的出现频率，把**最高频的对**合并成一个新单元。
3. 重复合并直到达到预设词表大小。

这样高频组合（如 `ing`、`tion`）被合并为整体，低频词自然被拆成子词。相关变体有 WordPiece（BERT）、Unigram/SentencePiece（多语言、无需预分词）。

### 2.3 为什么大模型用子词

- **词表可控**：几万个子词即可覆盖任意语言的任意词。
- **无 OOV**：任何词都能由子词拼出，最差回退到字符/字节。
- **跨语言**：字节级 BPE 可统一处理多语言与表情符号。

## 三、案例解析：BPE 合并过程演示

假设语料只有单词 `low, lower, newest, widest`，初始按字符切分。统计相邻对频率，`e` 与 `s` 常相邻（来自 `newest, widest`），于是首先合并：

1. 合并 `e s → es`：`new es t`, `wid es t`
2. 合并 `es t → est`：`new est`, `wid est`
3. 合并 `l o → lo`、`lo w → low` …

于是 `newest` 被切为 `new` + `est`，`widest` 被切为 `wid` + `est`——**`est` 这个高频后缀被复用**。当遇到训练时没见过的新词 `slowest`，也能被切成 `slow` + `est`，从而不再是"未登录词"。这正是子词切分兼顾词表大小与泛化能力的关键。

## 四、常见误区与边界

- **误区："token = 词"**：一个 token 可能是子词、单字符甚至一个字节；模型的"上下文长度"是按 token 计的。
- **误区："中文一定按字切最好"**：按字切丢失词义信息，实际多用子词/词混合策略。
- **边界**：切分粒度影响序列长度与计算成本，需在语义完整性与效率间权衡。

## 五、小结与延伸阅读

- 分词把文本切成 token；中文分词面临歧义与未登录词挑战。
- BPE 等子词方法用"高频合并"控制词表并消除 OOV，是现代大模型的标准做法。
- 下一步：[003 · 循环神经网络 RNN 与 LSTM](./003-循环神经网络RNN与LSTM.md)，建模 token 序列的时序依赖。
